---
template: testify
packages:
  github.com/seatgeek/mailroom:
    config:
      all: true
      dir: '{{ .InterfaceDir }}'
      filename: "mock_all.go"
      structname: Mock{{ .InterfaceName }}
      pkgname: '{{ .SrcPackageName }}'
      recursive: true
